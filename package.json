{
    "name": "cypress-ci",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "cypress:open": "cypress open",
        "cy:run:chrome": "cypress run --browser chrome",
        "test": "npx cypress run"
    },
    "devDependencies": {
        "cypress": "^4.5.0"
    }
}
